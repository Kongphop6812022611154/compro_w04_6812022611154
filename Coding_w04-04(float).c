#include <stdio.h>

int main() {
    float score = 90.5;                // float score ใช้สำหรับเก็บค่าทศนิยม
    printf("a: %f\n", score);

    return 0;
}

//1.score มีค่า = 90.500000
//2.หากใช้ float จะสามารถตอบเป็นค่าที่มีจุดทศนิยมได้
//3.สรุปคือ float เหมาะสำหรับผลลัพธ์ค่าที่เป็นจุดทศนิยม
